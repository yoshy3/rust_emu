# breakout

`cc65` / `ca65` / `ld65` で作る、最小構成のBreakout（ブロック崩し）サンプルです。

## ビルド
上位ディレクトリ（`game_examples`）で以下を実行します。

```bash
make build PROJECT=breakout
```

## 実行

```bash
make run PROJECT=breakout
```

操作:
- 左移動: 十字キー Left
- 右移動: 十字キー Right
